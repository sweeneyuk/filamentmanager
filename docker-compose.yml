version: '3.8'

services:
  app:
    build: .
    ports:
      - "4545:3000"
    volumes:
      - /mnt/fast/apps/filamentmanager:/app/server/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - DOMAIN=fm.msglover.me
